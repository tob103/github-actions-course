name: 01-Building Blocks
on:
  workflow_dispatch:
  pull_request:

jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: say-hello
        run: echo "Hello, World!"

  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: Successful step
        run: |
          echo "I will succeed"
          exit 0
      - name: Say goodbye
        run: echo "Goodbye!"


