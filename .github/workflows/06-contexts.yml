name: 06-Contexts
# The following line will display the name of the runner, but it's not valid in the context
#run-name: My custom workflow run name - $${runner.os}

run-name: 06 - Contexts | DEBUG- ${{inputs.debug}}
on:
  workflow_dispatch:
  #push:
    inputs:
      debug:
        type: boolean
        default: false
        description: "Enable debug mode"
env:
  MY_WORKFLOW_VAR: "workflow"
  MY_OVERWRITTEN_VAR: "workflow level"
jobs:
  echo-data:
    runs-on: ubuntu-latest
    env:
      MY_JOB_VAR: "job"
      MY_OVERWRITTEN_JOB_VAR: "job level"
    steps:
     - name: Display Information
       run: |
        echo "Event name: ${{ github.event_name }}"
        echo "Event action: ${{ github.event.action }}"
        echo "Event ref: ${{ github.ref }}"
        echo "Event sha: ${{ github.sha }}"
        echo "Actor: ${{ github.actor }}"
        echo "Workflow: ${{ github.workflow }}"
        echo "Run ID: ${{ github.run_id }}"
        echo "Run number: ${{ github.run_number }}"
        echo "Run attempt: ${{ github.run_attempt }}"
     - name: Retrieve Variable
       run: |
         echo " Repository variable: ${{vars.MY_VAR}}"
     - name: Print Env Varibles
       run: |
        echo "Workflow variable: ${{env.MY_WORKFLOW_VAR}}"
        echo "Overwritten workflow variable: ${{env.MY_OVERWRITTEN_VAR}}"
        echo "Job variable: ${{env.MY_JOB_VAR}}"
        echo "Overwritten variable: ${{env.MY_OVERWRITTEN_JOB_VAR}}"
         
         
         
